"use client";
import Link from "next/link";
import { useMemo, useState } from "react";
import { meals, midBases, motherBases } from "@/data/home-graph-v3";
export function MealBuilderV2(){
 const [mother,setMother]=useState("all");const [mid,setMid]=useState("all");
 const matches=useMemo(()=>meals.filter(m=>(mother==="all"||m.motherIds.includes(mother))&&(mid==="all"||m.midIds.includes(mid))),[mother,mid]);
 return <div className="hm-screen hm-builder-screen hm-v3-screen"><header className="hm-mobile-head"><div><span>FROM WHAT WE HAVE</span><h1>Find real combinations.</h1><p>No random recipe generator. Only directions already linked to our kitchen.</p></div><Link href="/cook" className="hm-head-help">‹</Link></header><section className="hm-builder-controls"><label><span>Mother base</span><select value={mother} onChange={e=>setMother(e.target.value)}><option value="all">Any mother</option>{motherBases.map(x=><option value={x.id} key={x.id}>{x.code} · {x.name}</option>)}</select></label><label><span>Mid-base</span><select value={mid} onChange={e=>setMid(e.target.value)}><option value="all">Any mid</option>{midBases.map(x=><option value={x.id} key={x.id}>{x.code} · {x.name}</option>)}</select></label></section><section className="hm-builder-result"><div className="hm-v3-section-head"><div><span>MATCHES</span><h2>{matches.length} real recipes</h2></div></div><div className="hm-recipe-grid">{matches.map(m=><Link href={`/cook/${m.id}`} key={m.id} className="hm-recipe-card">{m.image?<img src={m.image} alt=""/>:<div className="hm-meal-placeholder big">{m.title[0]}</div>}<div className="hm-recipe-card-copy"><div className="hm-code-row">{m.motherIds.map(id=><b key={id}>{motherBases.find(x=>x.id===id)?.code}</b>)}{m.midIds.map(id=><b key={id}>{midBases.find(x=>x.id===id)?.code}</b>)}</div><h2>{m.title}</h2><p>{m.subtitle}</p><footer><span>{m.cuisine}</span><span>{m.minutes} min</span></footer></div></Link>)}</div>{!matches.length&&<div className="hm-covered-card">We do not have an approved recipe for that exact combination yet. That becomes a recipe research job, not an AI guess.</div>}</section></div>
}
