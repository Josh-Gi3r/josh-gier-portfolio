import Link from "next/link";
import { midBases, motherBases } from "@/data/home-graph";
export function MidsV2(){return <div className="hm-screen hm-mids-screen"><header className="hm-page-head"><div><p className="hm-kicker">MID-BASES</p><h1>Sixteen multipliers</h1><p>Not random sauces. Each mid either sits on one or more mother bases, or stays standalone because a mother would add no value.</p></div><Link href="/prep" className="hm-btn secondary">Back to Prep</Link></header><div className="hm-mid-library">{midBases.map(mid=><Link href={`/prep/mids/${mid.id}`} key={mid.id} style={{"--tone":mid.tone} as React.CSSProperties}><i/><div className="hm-code-row"><b>{mid.code}</b>{mid.standalone&&<em>standalone</em>}</div><h2>{mid.name}</h2><p>{mid.parentMotherIds.length?`Links to ${mid.parentMotherIds.map(id=>motherBases.find(m=>m.id===id)?.code).join(" + ")}`:"No mother base required"}</p><footer><span>{mid.portionLabel}</span><span>×{mid.batchYield}</span></footer><small>{mid.examples.join(" · ")}</small></Link>)}</div></div>}
