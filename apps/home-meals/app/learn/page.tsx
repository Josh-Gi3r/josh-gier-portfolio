import Link from "next/link";
import { guides } from "@/data/home-meals";
import { CubeScale, FreezerMap, MealTransformation, SystemLayers } from "@/components/SystemInfographics";
export default function LearnPage() { return <div className="page"><header className="topbar"><div><span className="eyebrow">LEARN</span><h1>Understand the system in minutes.</h1><p>Visual operating instructions for the kitchen, not a wall of recipe-blog text.</p></div></header><SystemLayers/><div className="guide-grid">{guides.map((guide, i)=><Link href={`/learn/${guide.slug}`} key={guide.slug} className="guide-card"><div className={`guide-visual visual-${(i%4)+1}`}><span>{String(i+1).padStart(2,"0")}</span></div><div><span className="eyebrow">{guide.eyebrow}</span><h3>{guide.title}</h3><p>{guide.blurb}</p><span className="text-button">Open guide →</span></div></Link>)}</div><MealTransformation/><CubeScale/><FreezerMap/><section className="learning-placeholder"><span className="eyebrow">VISUAL PRODUCTION QUEUE</span><h2>Where generated images replace placeholders.</h2><div className="production-grid">{["8 mother-base hero photographs","Mother-base step sequences","Mid-base ingredient boards","18 finished meal photographs","Freezer organisation photographs","Technique / texture close-ups"].map((item,i)=><div key={item}><span>0{i+1}</span><strong>{item}</strong><small>Slot already locked into layout</small></div>)}</div></section></div>; }
