import Link from "next/link";
import { GroceryChecklist } from "@/components/GroceryChecklist";
import { storageRules } from "@/data/foundation";
import { foundationImages } from "@/data/foundation-assets";
export default function GroceriesPage(){return <div className="page groceries-page"><Link href="/prep" className="back-link">← Foundation</Link><header className="topbar"><div><span className="eyebrow">FOUNDATION SHOP</span><h1>Buy for the system, not one recipe.</h1><p>This is the first proper shop for the full prep architecture. Aromatics overlap heavily across mothers and mids so we buy in meaningful quantities, then freeze the labour rather than letting produce die in a humid pantry.</p></div><Link href="/prep/day" className="primary-button">Prep after shopping →</Link></header><section className="foundation-image-hero"><img src={foundationImages.groceries} alt="Home Meals foundation groceries"/><div><span className="eyebrow">ONE FOUNDATION SHOP</span><h2>High-overlap ingredients. Very little single-use clutter.</h2><p>Wet-market aromatics, Asian pantry staples, tomato, stock and measured freezer hardware form the base of the system.</p></div></section><GroceryChecklist/><section className="shop-storage-note"><span className="eyebrow">WHEN YOU GET HOME</span><div>{storageRules.slice(0,2).map(x=><article key={x.title}><strong>{x.title}</strong><b>{x.value}</b><p>{x.detail}</p></article>)}</div></section></div>}
