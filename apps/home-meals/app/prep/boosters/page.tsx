import Link from "next/link";
import { boosters } from "@/data/foundation";
export default function BoosterPage(){return <div className="page foundation-library"><Link href="/prep" className="back-link">← Foundation</Link><header className="topbar"><div><span className="eyebrow">BOOSTERS</span><h1>Small cubes. Fast corrections.</h1><p>These are not sauces. They are high-frequency micro-preps that add aroma, heat, umami, brightness or texture without forcing a whole cuisine.</p></div><span className="pill">{boosters.length} boosters</span></header><div className="library-grid booster-library">{boosters.map(x=><Link href={`/prep/boosters/${x.slug}`} key={x.slug} className="library-card" style={{"--base-color":x.tone} as React.CSSProperties}><i/><div><span>{x.code} · {x.portionMl} ml</span><h2>{x.name}</h2><p>{x.summary}</p></div><footer><small>Starter ×{x.starterYield}</small><b>→</b></footer></Link>)}</div></div>}
