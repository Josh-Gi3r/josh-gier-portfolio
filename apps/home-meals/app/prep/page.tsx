import Link from "next/link";
import { bases, boosters, inventoryGroups } from "@/data/home-meals";
import { CubeScale, FreezerMap } from "@/components/SystemInfographics";
import { Icon } from "@/components/Icons";
import { PrepLogger } from "@/components/PrepLogger";
export default function PrepPage() { return <div className="page"><header className="topbar"><div><span className="eyebrow">PREP</span><h1>Do the boring 70% once.</h1><p>Then cook fresh dinners instead of reheating meal-prep boxes.</p></div><PrepLogger/></header><div className="prep-summary"><div><small>LAST BATCH</small><strong>13 Sep</strong><span>42 portions added</span></div><div><small>NEXT SUGGESTION</small><strong>GREEN ×8</strong><span>Everything else can wait</span></div><div><small>DINNERS COVERED</small><strong>18+</strong><span>Across current freezer stock</span></div></div><section><div className="section-heading"><div><span className="eyebrow">MOTHER BASES</span><h2>The freezer engine.</h2></div></div><div className="base-card-grid">{bases.map((base) => { const stock = inventoryGroups.freezer.find((i) => i.name === base.code)?.qty; return <Link href={`/prep/${base.slug}`} className="base-card" key={base.code} style={{ "--base-color": base.tone } as React.CSSProperties}><div className="base-card-top"><i/><span>{base.code}</span><small>{base.size}</small></div><h3>{base.name}</h3><p>{base.summary}</p><div className="base-card-foot"><span>{stock ?? "—"} in freezer</span><Icon name="arrow"/></div></Link> })}</div></section><CubeScale/><section><div className="section-heading"><div><span className="eyebrow">BOOSTER CUBES</span><h2>Small cubes. Huge leverage.</h2></div></div><div className="booster-grid">{boosters.map((b) => <div key={b.code}><span>{b.code}</span><strong>{b.name}</strong><small>{b.size}</small></div>)}</div></section><FreezerMap/></div>; }
