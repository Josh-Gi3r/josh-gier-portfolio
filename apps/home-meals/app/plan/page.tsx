import { monthPool, recipeBySlug } from "@/data/home-meals";
import { PlanClient } from "@/components/PlanClient";
export default function PlanPage() { const groups = [{key:"favourites",label:"Favourites"},{key:"revisit",label:"Haven’t had recently"},{key:"newTests",label:"New tests"},{key:"emergency",label:"Emergency dinners"},{key:"weekend",label:"Weekend"}] as const; return <div className="page"><header className="topbar"><div><span className="eyebrow">PLAN</span><h1>A direction, not a prison.</h1><p>Monthly meal pool → weekly dinners → only the shopping we actually need.</p></div></header><PlanClient/><section className="month-pool"><div className="section-heading"><div><span className="eyebrow">SEPTEMBER POOL</span><h2>Meals worth choosing from.</h2></div><span className="pill">Living list</span></div><div className="pool-columns">{groups.map((group)=><div key={group.key}><span>{group.label}</span>{monthPool[group.key].map((slug)=>{const r=recipeBySlug(slug)!; return <div className="pool-item" key={slug}><strong>{r.title}</strong><small>{r.time}m · {r.base}</small></div>})}</div>)}</div></section></div>; }
